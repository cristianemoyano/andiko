{
  "buildCommand": "pnpm build",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "ignoreCommand": "! git diff HEAD^ HEAD --quiet -- src/ public/ next.config.ts package.json vercel.json",
  "git": {
    "deploymentEnabled": {
      "main": true,
      "develop": false
    }
  }
}

# Hoist pg and its transitive deps so Vercel's serverless file tracer can resolve
# require('pg') when sequelize loads the postgres dialect (pnpm isolated layout).
public-hoist-pattern[]=pg
public-hoist-pattern[]=pg-*
public-hoist-pattern[]=postgres-*

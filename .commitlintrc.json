{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "scope-enum": [2, "always", [
      "sales", "inventory", "purchases", "contacts", "accounting", "auth", "core",
      "catalog", "afip", "billing", "pos", "integrations", "communications", "storage",
      "logistics", "automations"
    ]],
    "scope-empty": [2, "never"]
  }
}

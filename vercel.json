{
  "buildCommand": "pnpm build",
  "installCommand": "rm -rf node_modules && pnpm install --frozen-lockfile --filter andiko...",
  "framework": "nextjs",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- src/ public/ next.config.ts package.json pnpm-lock.yaml vercel.json",
  "git": {
    "deploymentEnabled": {
      "main": true,
      "develop": false
    }
  }
}
